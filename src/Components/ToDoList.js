import React from 'react';

const TodoList = () => {
    return (
        <div className="todo-contatiner">
            <ul className="todo-list"></ul>
        </div>
    );
};

export default TodoList;